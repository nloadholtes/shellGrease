if [ -z "$1" ]
  then
    echo "No argumentis supplied, looking for:"
    echo "      Host, hostname, user, port"
    exit -1
fi

echo "
Host $1
Hostname $2
User $3
Port $4" >> ~/.ssh/config

ssh-copy-id $1
