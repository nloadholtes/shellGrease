#
# Help fetch/pull a Pull request from Github
if (( $# != 1 )); then
	echo "Need at least 1 param (the PR number). Can add 2nd param (the remote name)"
	exit
fi

SOURCE=${2:-upstream}
git fetch $SOURCE pull/$1/head:pr-$1
git co pr-$1
